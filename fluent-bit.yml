service:
  flush: 1
  log_level: info

pipeline:
  inputs:
    - name: forward
      listen: 0.0.0.0
      port: 5170
      buffer_chunk_size: 1M
      buffer_max_size: 6M

  filters:
    - name: stdout
      match: '*'

  outputs:
    - name: stdout
      match: '*'
